#include <stdio.h>#include <stdarg.h>//void st(int n);int sum (int n,...);int main(void){	/*	int x;	printf("please enter number : ");	scanf("%d",&x);	st(x);	//printf("Hello World !");	*/	int x=sum(3,1,2,5);	int y=sum(5,1,2,3,6,7);	int z=sum(4,1,2,3,4);	printf("x=%d\ny=%d\nz=%d\n",x,y,z);	return 0;		}/*void st(int n){	int arr[n];	int i;	for(i=0;i<n;i++)	{		arr[i]=i;	}	for(i=0;i<n;i++)	{		printf("%d\n",arr[i]);	}}*/int sum (int n,...){	int i,sum_res=0;	va_list ar; //creat pointer 	va_start(ar,n); //make pointer pointer to first elelment of maloc 	for(i=0;i<n;i++)	{		sum_res+=va_arg(ar,int);//move pointer but note no back or use pointer 	}	va_end(ar);//free element 	return sum_res;	}